dist: trusty
node_js: '12'
addons:
  chrome: 'stable'
language: node_js
before_install:
  - sudo apt-get update -q
before_script:
  - sudo ln -sf /etc/machine-id /var/lib/dbus/machine-id
  - export DISPLAY=:99.0
  - sh -e /etc/init.d/xvfb start
